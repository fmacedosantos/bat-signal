import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderWidth: 2,
    borderColor: 'black',
    padding: 10,
    borderRadius: 10,
    marginBottom: 20,
    width: 300
  }
});